# Backend dependencies for Hugging Face Spaces

# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Firebase
firebase-admin>=6.2.0

# ML/AI (optional - can use mock mode if not available)
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
bitsandbytes>=0.41.0

# Utilities
pydantic>=2.5.0
python-dotenv>=1.0.0
